{% if 'training' in app.host %}

{
  "ACCOUNTS_DOMAIN": "http://auth.{{ app.host }}",
  "API_ENDPOINT": "http://api.{{ app.host }}:8888",
  "APP_CLIENT_ID": "{{ db.client_id }}"
}

{% else %}

{
  "ACCOUNTS_DOMAIN": "http://{{ app.host }}",
  "API_ENDPOINT": "http://{{ app.host }}:8888",
  "APP_CLIENT_ID": "{{ db.client_id }}"
}

{% endif %}